---
interface Props {
  href: string;
  variant?: "primary" | "outline" | "ghost";
  class?: string;
}

const { href, variant = "primary", class: className } = Astro.props;

const base =
  "inline-flex items-center justify-center gap-2 rounded-full px-6 py-3 text-sm font-semibold transition-all focus-visible:outline-2 focus-visible:outline-offset-2";

const variants: Record<NonNullable<Props["variant"]>, string> = {
  primary:
    "bg-accent text-accent-foreground shadow-[var(--shadow-soft)] hover:bg-accent-hover hover:-translate-y-0.5 hover:shadow-[var(--shadow-lift)]",
  outline:
    "border border-border bg-surface text-foreground hover:border-accent hover:text-accent",
  ghost: "text-accent hover:text-accent-hover",
};
---

<a href={href} class:list={[base, variants[variant], className]}>
  <slot />
</a>
